\begin{mathpar}
    \inferrule* [Right=DGSoundness]
    {
        \dgthoare{~}{\grad{\phi_1}}{\grad{s}}{\grad{\phi_2}} \\ 
        \grad{\pi_{\grad{s}}} \,\in \setGProgramState_{\grad{s}}\\
        \evalgphiGen{\grad{\pi_{\grad{s}}}}{\grad{\phi_1}}
    }
    {
        \evalgphiGen{\gsssem^{\grad{s}}(\grad{\pi_{\grad{s}}})}{\grad{\phi_2}}
    }
\end{mathpar}

% talk about \gthoare

Consider $\phi_{pre}, \phi_{post} \in \setFormula$ and $\pi_{pre} \in \setStmt_s$ and $$ and $\evalphiGen{\pi_s}{\phi_{pre}}$.

$\thoare{~}{\phi_{pre}}{s}{\phi_{post}}$
 - yes
 - no
     - 

3 cases (for terminating piece of code):
    statically verifiable and progress and preservation
not statically verifiable and progress
not statically verifiable and no progress


    gradually verifiable and progress and preservation
not gradually verifiable and progress
not gradually verifiable and no progress


There are several ways to achieve this (variation whenever static reasoning impossible: progress exception vs. success)!
Examples:




{i = 10000}
n = collatzIterations(300, i);
{1 <= n * n <= 4}
{n = 4}
staticAssert (n = 4);
{n = 4}
